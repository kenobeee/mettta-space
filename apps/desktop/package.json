{
  "name": "desktop",
  "version": "0.0.1",
  "private": true,
  "description": "Desktop shell for mettta.space (Tauri)",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "node scripts/tauri-build.js",
    "build:macos": "node scripts/build-web.js && node scripts/tauri-build.js --bundles dmg",
    "build:windows": "node scripts/build-web.js && node scripts/tauri-build.js --bundles nsis",
    "build:web": "node scripts/build-web.js"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
